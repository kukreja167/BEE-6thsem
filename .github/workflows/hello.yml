name: My First Workflow

on: push

jobs:
  say-hello:
    runs-on: ubuntu-latest
      
    steps:
      - name: checkout code
        uses: actions/checkout@v4
  say-welcome:
    needs: say-hello
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: print message
        run: echo "Welcome kavya"
        
