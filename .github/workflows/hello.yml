name: My First Workflow

on: push

jobs:
  say-hello:
    runs-on: ubuntu-latest
      
    steps:
      - name: Print message 
        run: echo "Hello, Github Actions!"
      - name: my name
        run: echo "Hello, Kavya!"
      - name: your name
        run: echo "Hello, Sargun!"
  say-welcome:
    needs: say-hello
    runs-on: ubuntu-latest

    steps:
      - name: print message
        run: echo "Welcome kavya"
        
